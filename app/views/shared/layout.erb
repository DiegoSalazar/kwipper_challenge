<!DOCTYPE html>
<html lang="en">
<head>
  <%= render "shared/head" %>
</head>
<body role="document">
  <%= render "shared/nav" %>

  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <%= render "shared/flash" if @flash %>
      </div>
    </div>

    <div class="row">
      <div class="col-sm-2"></div>

      <div class="col-sm-8">
        <%= @view %>
      </div>

      <div class="col-sm-2"></div>
    </div>
  </div>

  <%= render "shared/footer" %>
</body>
</html>